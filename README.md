| Paquete | Comando de instalación | Descripción corta |
|---|---:|---|
| sl | pkg install sl -y | Animación de tren; broma visual si se escribe mal `ls`. |
| pv | pkg install pv -y | Muestra progreso de pipes y transferencia de datos. |
| python | pkg install python -y | Intérprete Python 3 (desarrollo y scripts). |
| python2 | pkg install python2 -y | Intérprete Python 2 (obsoleto en muchos casos). |
| cowsay | pkg install cowsay -y | Dibuja mensajes en ASCII con una vaca u otros figuras. |
| unzip | pkg install unzip -y | Descomprimir archivos ZIP. |
| zip | pkg install zip -y | Comprimir archivos en formato ZIP. |
| tsu | pkg install tsu -y | Ejecutor de comandos como root (puede requerir configuración de permisos). |
| screenfetch | pkg install screenfetch -y | Muestra información del sistema en ASCII al abrir la terminal. |
| git | pkg install git -y | Control de versiones y clonación de repositorios. |
| figlet | pkg install figlet -y | Genera texto grande en ASCII (titulares). |
| toilet | pkg install toilet -y | Similar a figlet; texto grande y con efectos. |
| vim | pkg install vim -y | Editor de texto avanzado (modo modal). |
| util-linux | pkg install util-linux -y | Utilidades básicas del sistema (varios comandos). |
| ncurses-utils | pkg install ncurses-utils -y | Herramientas de consola (por ejemplo `tput`). |
| perl | pkg install perl -y | Lenguaje de scripting/interpretes para muchas herramientas. |
| php | pkg install php -y | Intérprete PHP para scripts y pequeñas APIs. |
| nano | pkg install nano -y | Editor de texto sencillo y amigable. |
| xapian-tools | pkg install xapian-tools -y | Herramientas relacionadas con el motor de búsqueda Xapian. |
| wget | pkg install wget -y | Descargas HTTP/FTP desde la terminal. |
| coreutils | pkg install coreutils -y | Herramientas GNU básicas (ls, cp, mv, etc.). |
| openssh | pkg install openssh -y | Cliente y servidor SSH (ssh, sshd, scp, sftp). |
| w3m | pkg install w3m -y | Navegador web en terminal y visualizador de HTML. |
| htop | pkg install htop -y | Monitor interactivo de procesos. |
| termux-tools | pkg install termux-tools -y | Herramientas específicas de Termux. |
| nmap | pkg install nmap -y | Escáner de redes y puertos. |
| curl | pkg install curl -y | Cliente HTTP/FTP con soporte para scripts. |
| tsudo | pkg install tsudo -y | Alternativa a sudo adaptada para Termux. |
| ruby | pkg install ruby -y | Lenguaje Ruby (scripts y gemas). |
| lolcat | pkg install lolcat -y | Colorea la salida de texto; efecto visual. |
| requests | pip3 install requests | Librería Python para hacer solicitudes HTTP. |
| colorama | pip3 install colorama | Librería Python para colorear texto en terminal. |
| tmux | pkg install tmux -y | Multiplexor de terminales (sesiones persistentes). |
| screen | pkg install screen -y | Multiplexor de terminales alternativo a tmux. |
| glow | pkg install glow -y | Visualizador de Markdown en terminal. |
| aria2 | pkg install aria2 -y | Descargador rápido con múltiples fuentes/segmentos. |
| neofetch | pkg install neofetch -y | Muestra información del sistema similar a screenfetch. |
| hollywood | pkg install hollywood -y | Montaje visual con múltiples ventanas para "efecto cine". |
| aircrack-ng | pkg install aircrack-ng -y | Suite para auditoría de redes Wi‑Fi (requiere hardware compatible). |
| john | pkg install john -y | Herramienta para recuperación de contraseñas (John the Ripper). |
| metasploit | pkg install metasploit -y | Framework de pruebas de penetración (puede requerir instalación adicional/no disponible en repositorios oficiales). |
| nessus | pkg install nessus -y | Escáner de vulnerabilidades (puede no estar disponible directamente en Termux). |
| burpsuite | pkg install burpsuite -y | Plataforma de pruebas de seguridad web (normalmente Java, puede requerir instalación adicional). |
| tcpdump | pkg install tcpdump -y | Captura y análisis de paquetes de red. |
| java | pkg install openjdk -y | Entorno de ejecución Java (OpenJDK). |
| nodejs | pkg install nodejs -y | Entorno de ejecución Node.js para JavaScript. |
| npm | pkg install npm -y | Gestor de paquetes de Node.js. |
| yarn | pkg install yarn -y | Gestor de paquetes alternativo para Node.js. |
| pip | pkg install python-pip -y | Gestor de paquetes Python 2 (posiblemente obsoleto). |
| pip3 | pkg install python3-pip -y | Gestor de paquetes Python 3. |
| gcc | pkg install gcc -y | Compilador GNU C/C++. |
| clang | pkg install clang -y | Compilador LLVM/Clang. |
| make | pkg install make -y | Herramienta de automatización de compilaciones. |
| cmake | pkg install cmake -y | Sistema de construcción multiplataforma. |
| git-clone | pkg install git-clone -y | (Nota: `git-clone` no es normalmente un paquete; el comando es `git clone`.) |
| svn | pkg install svn -y | Cliente Subversion (control de versiones). |
| mercurial | pkg install mercurial -y | Cliente Mercurial (control de versiones). |
| zipalign | pkg install zipalign -y | Alineación de APKs (herramienta de Android; puede no estar en repositorios estándar). |
| apktool | pkg install apktool -y | Herramienta para descompilar/analizar APKs de Android. |
| dex2jar | pkg install dex2jar -y | Convierte .dex a .jar para análisis de Java/Android. |
| jd-gui | pkg install jd-gui -y | Visor GUI para archivos .jar/Java (puede necesitar entorno gráfico). |
| ncat | pkg install ncat -y | Herramienta de conexión de red (ncat/netcat de nmap). |
| netcat | pkg install netcat -y | Utilidad de red para conexiones TCP/UDP. |
| socat | pkg install socat -y | Enlaza canales de datos entre puertos/socket/archivos. |
| openssl | pkg install openssl -y | Biblioteca y utilidades criptográficas (openssl). |
| ssh-keygen | pkg install openssh -y | Generación de pares de claves SSH (parte de OpenSSH). |
| ssh-agent | pkg install openssh -y | Agente para gestionar claves SSH en memoria (parte de OpenSSH). |
| ssh-add | pkg install openssh -y | Añadir claves al agente SSH (parte de OpenSSH). |
